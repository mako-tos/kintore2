# タスクリスト: 共通レイアウトとナビ�ネーション

**機能**: 共通レイアウトとナビゲーション  
**作成日**: 2025-11-24  
**ステータス**: Draft

---

## Phase 1: 基本レイアウトとヘッダー構造

### 1.1 Layout コンポーネント作成

- [ ] `src/components/Layout.tsx` ファイルを作成
- [ ] Header と main コンテンツエリアを含む基本構造を実装
- [ ] TypeScript 型定義を追加（Props に children）
- [ ] Pure.css のグリッド構造と整合性を確認

### 1.2 Header コンポーネント作成

- [ ] `src/components/Header.tsx` ファイルを作成
- [ ] アプリケーション名/ロゴ表示エリアを実装
- [ ] PC 向け横並びナビゲーションの基本構造を作成
- [ ] TypeScript 型定義を追加

### 1.3 \_app.tsx への統合

- [ ] `pages/_app.tsx` を修正し、全ページに `<Layout>` を適用
- [ ] 既存ページ（index, training-menus/new, training-records/new）が正しく表示されることを確認
- [ ] レイアウトシフトがないことを確認

---

## Phase 2: ナビゲーション機能実装

### 2.1 Navigation コンポーネント作成

- [ ] `src/components/Navigation.tsx` ファイルを作成
- [ ] ナビゲーション項目の配列を定義
  - [ ] ホーム: `/`
  - [ ] カレンダー表示: `/calendar`（将来実装予定）
  - [ ] メニュー一覧: `/training-menus`（将来実装予定）
  - [ ] メニュー新規作成: `/training-menus/new`
  - [ ] 記録登録: `/training-records/new`
- [ ] `next/link` を使用したリンク実装
- [ ] TypeScript 型定義を追加

### 2.2 アクティブページのハイライト

- [ ] `useRouter` をインポートして現在パスを取得
- [ ] 現在のパスとリンクパスを比較するロジックを実装
- [ ] アクティブ状態のリンクに `active` クラスを適用

### 2.3 スタイリング

- [ ] `src/styles/layout.css` ファイルを作成
- [ ] ヘッダーの基本スタイルを定義
  - [ ] 背景色、高さ、padding
  - [ ] 左端にロゴ、右側にナビゲーション配置
- [ ] ナビゲーションリンクのスタイルを定義
  - [ ] 横並び表示
  - [ ] ホバー時のスタイル
  - [ ] アクティブ状態のスタイル（背景色やアンダーライン）
- [ ] `_app.tsx` で `layout.css` をインポート

---

## Phase 3: レスポンシブ対応

### 3.1 ハンバーガーメニュー実装

- [ ] ハンバーガーメニューアイコン（☰）を追加
- [ ] メニュー開閉状態を `useState` で管理（`isMenuOpen`）
- [ ] トグルボタンのクリックイベントを実装
- [ ] PC 表示時はハンバーガーアイコンを非表示

### 3.2 ドロワーメニュー実装

- [ ] メニュー展開時のオーバーレイ背景を追加
- [ ] ナビゲーションリンクを縦並びで表示
- [ ] リンククリック時にメニューを自動で閉じる処理を追加
- [ ] オーバーレイクリック時にメニューを閉じる処理を追加

### 3.3 メディアクエリ設定

- [ ] `layout.css` にメディアクエリを追加
  - [ ] `@media (min-width: 769px)`: PC 表示用スタイル
  - [ ] `@media (max-width: 768px)`: スマホ表示用スタイル
- [ ] PC 表示で横並びナビゲーションを表示
- [ ] スマホ表示でハンバーガーメニューとドロワーを表示
- [ ] 各ブレークポイントで表示を確認

---

## Phase 4: テストとリファクタリング

### 4.1 コンポーネント単体テスト

- [ ] `src/components/__tests__/Header.test.tsx` を作成
- [ ] ヘッダーとロゴが正しくレンダリングされることをテスト
- [ ] ナビゲーションリンクが正しくレンダリングされることをテスト
- [ ] アクティブ状態が正しく適用されることをテスト（モック useRouter）
- [ ] すべてのテストがパスすることを確認

### 4.2 E2E 的な動作確認

- [ ] 各ページへの遷移が正しく動作することを手動テスト
- [ ] ヘッダーがすべてのページで表示されることを確認
- [ ] PC 表示でのナビゲーション動作確認
- [ ] スマホ表示（ブラウザ開発者ツール）でのハンバーガーメニュー動作確認
- [ ] 実機（スマートフォン）での動作確認

### 4.3 パフォーマンス最適化

- [ ] 不要な再レンダリングを確認（React DevTools Profiler）
- [ ] 必要に応じて `React.memo` を適用
- [ ] CSS の最適化（不要なスタイルの削除）
- [ ] バンドルサイズへの影響を確認

---

## 追加タスク（オプション）

### アクセシビリティ向上

- [ ] ナビゲーションリンクに `aria-label` を追加
- [ ] ハンバーガーメニューボタンに `aria-expanded` を追加
- [ ] キーボード操作（Tab, Enter）でメニュー開閉できることを確認
- [ ] スクリーンリーダーでの動作確認（可能であれば）

### ドキュメント作成

- [ ] `README.md` に共通レイアウトの使用方法を追記
- [ ] コンポーネントに JSDoc コメントを追加
- [ ] 日本語コメントで実装意図を説明

---

## 完了条件

すべての Phase が完了し、以下が満たされていること:

- ✅ すべてのページで共通ヘッダーが表示される
- ✅ ナビゲーションリンクから各ページに遷移できる
- ✅ 現在ページがナビゲーション上で視覚的に識別できる
- ✅ PC/スマホ両方で適切に表示される
- ✅ スマホでハンバーガーメニューが正しく動作する
- ✅ すべてのテストがパスする
- ✅ パフォーマンス上の問題がない

---
