# タスクリスト: カレンダー表示機能

**機能**: カレンダー表示機能  
**作成日**: 2025-11-24  
**ステータス**: Draft

---

## Phase 1: カレンダーページの基本構造

### 1.1 カレンダーページ作成

- [ ] `pages/calendar.tsx` ファイルを作成
- [ ] 基本的なページ構造を実装（Layout で包む）
- [ ] ページタイトル「カレンダー」を表示
- [ ] ヘッダーエリアのプレースホルダー作成

### 1.2 カレンダーユーティリティ関数

- [ ] `src/utils/calendar.ts` ファイルを作成
- [ ] `getDaysInMonth(year, month)`: 月の日数を取得
- [ ] `getFirstDayOfMonth(year, month)`: 月初日の曜日を取得（0=日曜）
- [ ] `generateCalendarGrid(year, month)`: カレンダーグリッド配列を生成
- [ ] `formatDateKey(date)`: YYYY-MM-DD 形式の文字列に変換
- [ ] ユーティリティ関数の TypeScript 型定義

### 1.3 型定義

- [ ] `src/types/calendar.ts` ファイルを作成
- [ ] `CalendarDay` 型を定義（date, isCurrentMonth, isToday, isPast, hasRecords）
- [ ] `RecordsByDate` 型を定義（Record<string, TrainingRecord[]>）
- [ ] `MonthYear` 型を定義（year, month）

---

## Phase 2: カレンダーグリッド表示

### 2.1 Calendar コンポーネント作成

- [ ] `src/components/Calendar.tsx` ファイルを作成
- [ ] 現在表示中の年月を `useState` で管理
- [ ] カレンダーユーティリティを使用してグリッド生成
- [ ] 7列×週数のグリッドを `<table>` でレンダリング
- [ ] 曜日ヘッダー（日月火水木金土）を表示
- [ ] TypeScript Props 型定義

### 2.2 CalendarDay コンポーネント作成

- [ ] `src/components/CalendarDay.tsx` ファイルを作成
- [ ] Props を定義（day: CalendarDay, onClick）
- [ ] 日付番号を表示
- [ ] 現在日の場合はハイライトクラスを適用
- [ ] 前月・次月の日付にグレーアウトクラスを適用
- [ ] 記録がある場合は⭐マークを表示（未実装時はプレースホルダー）
- [ ] クリックイベントハンドラを設定

### 2.3 スタイリング

- [ ] `src/styles/calendar.css` ファイルを作成
- [ ] カレンダー全体のコンテナスタイル
- [ ] テーブルのスタイル（枠線、セル幅、高さ）
- [ ] 曜日ヘッダーのスタイル
- [ ] 日付セルの基本スタイル
- [ ] 現在日のハイライトスタイル（背景色、枠線）
- [ ] 前月・次月の日付のグレーアウトスタイル
- [ ] 土曜日（青色）・日曜日（赤色）のテキスト色（オプション）
- [ ] `pages/calendar.tsx` で `calendar.css` をインポート

---

## Phase 3: 記録データ取得と★マーク表示

### 3.1 記録データ取得

- [ ] `pages/calendar.tsx` に記録データ取得ロジックを追加
- [ ] 表示月の開始日・終了日を計算
- [ ] `useEffect` で API リクエスト実行
- [ ] `apiClient.get<{ records: TrainingRecord[]; total: number }>` で取得
- [ ] ローディング状態を `useState` で管理
- [ ] エラー状態を `useState` で管理

### 3.2 記録データの集約

- [ ] API レスポンスを日付ごとにグループ化する関数を作成
- [ ] `groupRecordsByDate(records)` 関数を実装
- [ ] キー: YYYY-MM-DD 形式、値: TrainingRecord[]
- [ ] 集約データを state に保存

### 3.3 ⭐マーク表示

- [ ] `CalendarDay` コンポーネントで `hasRecords` prop を受け取る
- [ ] 記録がある場合に⭐マークを表示
- [ ] ⭐マークのスタイル（サイズ、配置）
- [ ] クリック可能なスタイル（cursor: pointer）
- [ ] ホバー時のスタイル（背景色変更など）

### 3.4 ローディング・エラー表示

- [ ] ローディング中は `LoadingSpinner` コンポーネントを表示
- [ ] エラー時は `ErrorMessage` コンポーネントを表示
- [ ] エラー時の再試行ボタンを追加（オプション）

---

## Phase 4: ポップアップ実装

### 4.1 RecordPopup コンポーネント作成

- [ ] `src/components/RecordPopup.tsx` ファイルを作成
- [ ] Props を定義（date, records, onClose）
- [ ] オーバーレイ要素を作成
- [ ] ポップアップコンテンツ要素を作成
- [ ] 日付ヘッダーを表示（YYYY年MM月DD日）
- [ ] メニューリストを表示（メニュー名: 回数回）
- [ ] 閉じるボタンを追加

### 4.2 ポップアップ開閉制御

- [ ] `Calendar` コンポーネントで選択日を `useState` で管理
- [ ] ⭐マーククリック時に選択日をセット
- [ ] 選択日が null でない場合に `RecordPopup` をレンダリング
- [ ] オーバーレイクリック時に選択日を null にセット
- [ ] 閉じるボタンクリック時に選択日を null にセット

### 4.3 スタイリング

- [ ] オーバーレイのスタイル（position: fixed, 半透明黒背景）
- [ ] ポップアップのスタイル（中央配置、白背景、影、角丸）
- [ ] 日付ヘッダーのスタイル（フォントサイズ、太字）
- [ ] メニューリストのスタイル（リスト形式、余白）
- [ ] 閉じるボタンのスタイル（Pure.css ボタン）
- [ ] z-index の設定（オーバーレイ: 400, ポップアップ: 500）

---

## Phase 5: 月間移動機能

### 5.1 前月/次月ボタン実装

- [ ] `Calendar` コンポーネントに前月ボタンを追加
- [ ] `Calendar` コンポーネントに次月ボタンを追加
- [ ] 年月表示（YYYY年MM月）を追加
- [ ] 前月ボタンクリック時に表示月を1ヶ月前に変更
- [ ] 次月ボタンクリック時に表示月を1ヶ月後に変更
- [ ] 次月ボタンの有効/無効判定ロジック

### 5.2 月変更時のデータ再取得

- [ ] `useEffect` の依存配列に year, month を追加
- [ ] 月変更時に記録データを再取得
- [ ] ローディング状態を表示

### 5.3 年月の境界処理

- [ ] 1月の前月 → 前年12月の処理を実装
- [ ] 12月の次月 → 翌年1月の処理を実装
- [ ] 月変更関数のテストケースを追加

### 5.4 スタイリング

- [ ] ヘッダー部分のレイアウト（年月中央、ボタン両端）
- [ ] 前月/次月ボタンのスタイル
- [ ] 無効化ボタンのスタイル（グレーアウト、cursor: not-allowed）

---

## Phase 6: レスポンシブ対応とUI改善

### 6.1 レスポンシブスタイリング

- [ ] PC表示（> 768px）のスタイル調整
- [ ] タブレット表示（481px〜768px）のスタイル調整
- [ ] スマホ表示（<= 480px）のスタイル調整
- [ ] セルサイズ、フォントサイズの段階的縮小
- [ ] タップ領域の確保（最低 44px×44px）

### 6.2 アクセシビリティ

- [ ] 前月/次月ボタンに `aria-label` を追加
- [ ] ポップアップに `role="dialog"` を追加
- [ ] ポップアップに `aria-labelledby` でタイトル参照
- [ ] Escキーでポップアップを閉じる機能（オプション）
- [ ] フォーカス管理（ポップアップ表示時にフォーカス移動、オプション）

### 6.3 UI/UX 改善

- [ ] ローディングスピナーの配置改善
- [ ] エラーメッセージの表示改善
- [ ] 再試行ボタンの追加
- [ ] 今月に戻るボタンの追加（オプション）
- [ ] ⭐マークホバー時のツールチップ（簡易情報表示、オプション）
- [ ] ポップアップのフェードイン/アウトアニメーション（オプション）

---

## Phase 7: テストと最適化

### 7.1 ユーティリティ関数テスト

- [ ] `src/utils/__tests__/calendar.test.ts` を作成
- [ ] `getDaysInMonth` のテスト（各月、うるう年）
- [ ] `getFirstDayOfMonth` のテスト
- [ ] `generateCalendarGrid` のテスト
- [ ] `formatDateKey` のテスト
- [ ] エッジケースのテスト（2月、月境界）

### 7.2 コンポーネント単体テスト

- [ ] `src/components/__tests__/Calendar.test.tsx` を作成
- [ ] カレンダーグリッドのレンダリングテスト
- [ ] 現在日のハイライトテスト
- [ ] ★マーク表示のテスト
- [ ] 前月/次月ボタンのテスト

### 7.3 E2E 的な動作確認

- [ ] 実データでカレンダー表示確認
- [ ] ⭐クリック → ポップアップ表示確認
- [ ] ポップアップ閉じる動作確認
- [ ] 月移動 → データ再取得確認
- [ ] PC/タブレット/スマホでの表示確認
- [ ] 実機（スマートフォン）での動作確認

### 7.4 パフォーマンス最適化

- [ ] 不要な再レンダリングを確認（React DevTools Profiler）
- [ ] `CalendarDay` に `React.memo` を適用
- [ ] グリッド生成を `useMemo` でメモ化
- [ ] API リクエストのキャッシュ検討
- [ ] 大量記録時のポップアップ表示速度確認

---

## 追加タスク（オプション）

### 拡張機能

- [ ] 記録のない日をクリックして直接記録登録画面へ遷移
- [ ] ポップアップ内に「記録追加」ボタンを配置
- [ ] カレンダーに合計回数を表示（⭐の横に数字など）
- [ ] 週の開始曜日を設定可能にする（月曜始まりなど）

### ドキュメント

- [ ] `README.md` にカレンダー機能の説明を追記
- [ ] コンポーネントに JSDoc コメントを追加
- [ ] 日本語コメントで実装意図を説明

---

## 完了条件

すべての Phase が完了し、以下が満たされていること:

- ✅ 現在月のカレンダーが表示される
- ✅ トレーニング記録がある日に⭐マークが表示される
- ✅ ⭐クリックでポップアップが表示され、記録詳細が確認できる
- ✅ 前月ボタンで過去に遡れる
- ✅ 次月ボタンは現在月を超えて進めない
- ✅ PC/スマホ両方で快適に表示・操作できる
- ✅ すべてのテストがパスする
- ✅ パフォーマンス上の問題がない

---
